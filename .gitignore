.DS_Store
__pycache__/
*.pyc

# The redaction tooling is NOT published. build.py and scan.py together
# form an index of exactly what was removed and why — the excluded pages
# with their reasons, the internal technology names, the literal values
# substituted out. Publishing them would undo the redaction they perform.
tools/
